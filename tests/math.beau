object math = order("math")
number customPi = 4*((4*math.atan(1/5))-(math.atan(1/239)))
if customPi equals math.pi {
  io.out("it equals!")
  io.out("here's my custom pi: " + customPi)
  io.out("here's the native pi: " + math.pi)
} else {
  io.out("uhhhhmmm...")
}
